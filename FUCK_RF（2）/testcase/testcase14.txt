后台，公众号消息、服务通知消息的再整理
1、公众号消息、服务通知消息的种类保持一致
2、接受消息的种类分成如下几种：
系统消息：加入工作圈
任务消息：设定执行者、执行者修改、修改截止时间、添加关注者、任务标题修改、任务完成状态、任务评论、任务即将到期提醒（自动）、任务提醒（用户设置）、删除任务
3、可以设置接受哪些类型的消息，默认选择：
设定执行者、执行者修改、任务评论、任务即将到期提醒（到期前一小时，自动提醒）、任务提醒（用户设置）、删除任务